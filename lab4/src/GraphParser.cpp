#include "GraphParser.h"

Graph GraphParser::parseFromStream(std::istream& input)
{
    Graph graph;
    graph.clear();

    // Read line by line, write graph[first][second] = weight

    return graph;
}
